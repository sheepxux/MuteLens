# GISTER 社交平台接入配置
# 复制此文件为 .env，填入你的凭证，不要把 .env 提交到 Git

# ── RSSHub 实例 ──────────────────────────────────────────────
# 默认使用公共实例 https://rsshub.app
# 建议自托管（Docker 一行命令）以避免公共实例限速：
#   docker run -d -p 1200:1200 diygod/rsshub
RSSHUB_BASE=https://rsshub.app

# ── Twitter/X Cookie ─────────────────────────────────────────
# 获取方法：
# 1. 浏览器登录 Twitter/X（x.com）
# 2. 打开 DevTools（F12）→ Network → 随便点一个请求
# 3. 复制 Request Headers 中的 Cookie 字段值（一整行，不要换行）
TWITTER_COOKIE=
